::1-1:: 以下关于函数的描述错误的是 {
    =一个完整的python程序一定要有自定义函数
    ~Python中的函数用 单词 + 括号 的形式来表示
    ~自定义函数不一定能提高程序的运行效率
    ~scratch中的自制积木就是函数
}

::1-2:: 自定义函数用哪个关键字 {
    =def
    ~del
    ~is
    ~fun
}

::1-3:: 下列关于函数的描述正确的是？ {
    =函数是可重复使用的，用来实现单一，或相关联功能的代码段
    ~函数中必须return语句
    ~函数中必须有参数
    ~I函数内容以冒号起始，可以不缩进
}

// def f():
//     c=0
//     for i in range(10):
//         c = i
//     return c
// print(f())
::1-4:: 运行以下代码，正确的打印结果是？ {
    =9
    ~10
    ~0
    ~2
}


